# =============================================================================
# pnpm Workspace Configuration
# =============================================================================
#
# Declares the workspace packages for pnpm.
# pnpm does NOT read the "workspaces" field in package.json — this file
# is the canonical source of truth when using pnpm as the package manager.
#
# All packages listed here can reference each other via workspace:* protocol.
#
# Docs: https://pnpm.io/pnpm-workspace_yaml
# =============================================================================

packages:
  # ── Deployable Applications ────────────────────────────────────────────────
  # Vite app (web), Electron desktop app
  - "apps/*"

  # ── Shared Packages / Libraries ────────────────────────────────────────────
  # cache, config, desktop, events, kbd, logger, pwa, redis, rxdb-eloquent,
  # settings, support, theming, ui, refine, multitenancy
  - "packages/*"
